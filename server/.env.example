# The port number on which the Express server will run.
PORT=5000

# The connection string for the MongoDB Atlas database instance.
MONGODB_URI=

# A secure secret key (minimum 32 characters) used to sign and verify JSON Web Tokens (JWT) for authentication.
JWT_SECRET=

# The API key required to access Google's Gemini AI services/models.
GEMINI_API_KEY=

# The URL of the client frontend application (for CORS configuration).
CLIENT_URL=http://localhost:5173
